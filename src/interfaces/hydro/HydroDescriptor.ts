/**
 * Payload descriptors.
 *
 * * This is the part of the common header that describes the payload type.
 *
 * @since 1.1.6
 */
export enum HydroDescriptor {
  Calcdex = 'calcdex',
  Presets = 'presets',
  Settings = 'settings',
}
