/**
 * HTML `&times;` entity, represented as `U+00D7`.
 *
 * @example '×'
 * @since 1.0.3
 */
export const times = '\u00D7';

/**
 * HTML `&plusmn;` entity, represented as `U+00B1`.
 *
 * @example '±'
 * @since 1.0.7
 */
export const plusmn = '\u00B1';

/**
 * HTML `&eacute;` entity, represented as `U+00E9`.
 *
 * @example 'é'
 * @since 1.0.3
 */
export const eacute = '\u00E9';

/**
 * Bullet operator (not an HTML entity), represented as `U+2219`.
 *
 * @example '∙'
 * @since 1.0.4
 */
export const bullop = '\u2219';

/**
 * HTML `&bull;` entity, represented as `U+2022`.
 *
 * @example '•'
 * @since 1.0.4
 */
export const bull = '\u2022';

/**
 * HTML `&lsaquo;` entity, represented as `U+2039`.
 *
 * @example '‹'
 * @since 1.0.4
 */
export const lsaquo = '\u2039';

/**
 * HTML `&rsaquo;` entity, represented as `U+203A`.
 *
 * @example '›'
 * @since 1.0.4
 */
export const rsaquo = '\u203A';

/**
 * HTML `&laquo;` entity, represented as `U+00AB`.
 *
 * @example '«'
 * @since 1.0.4
 */
export const laquo = '\u00AB';

/**
 * HTML `&raquo;` entity, represented as `U+00BB`.
 *
 * @example '»'
 * @since 1.0.4
 */
export const raquo = '\u00BB';

/**
 * HTML `&larr;` entity, represented as `U+2190`.
 *
 * @example '←'
 * @since 1.0.6
 */
export const larr = '\u2190';

/**
 * HTML `&uarr;` entity, represented as `U+2191`.
 *
 * @example '↑'
 * @since 1.0.6
 */
export const uarr = '\u2191';

/**
 * HTML `&rarr;` entity, represented as `U+2192`.
 *
 * * rawr XD
 *
 * @example '→'
 * @since 1.0.6
 */
export const rarr = '\u2192';

/**
 * HTML `&darr;` entity, represented as `U+2193`.
 *
 * @example '↓'
 * @since 1.0.6
 */
export const darr = '\u2193';

/**
 * HTML `&nwarr;` entity, represented as `U+2196`.
 *
 * @example '↖'
 * @since 1.0.6
 */
export const nwarr = '\u2196';

/**
 * HTML `&nearr;` entity, represented as `U+2197`.
 *
 * @example '↗'
 * @since 1.0.6
 */
export const nearr = '\u2197';

/**
 * HTML `&searr;` entity, represented as `U+2198`.
 *
 * @example '↘'
 * @since 1.0.6
 */
export const searr = '\u2198';

/**
 * HTML `&swarr;` entity, represented as `U+2199'
 *
 * @example '↙'
 * @since 1.0.6
 */
export const swarr = '\u2199';
